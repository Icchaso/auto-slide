---
name: slidesmith
description: >-
  テキスト（台本・メモ・箇条書き）からプロ品質の16:9スライドを作るスキル。
  AIは deck.json に「型」と「言葉」を書くだけで、描画・機械検品・PPTX化はスクリプトが行う（安いモデルでも品質がぶれない）。
  29の型（表紙・課題・巨大数字・数字の並び・比較・手順・グラフ・表・概要・料金・ファネル・お客様の声・チーム・ワーク・チェックリストなど）と23テーマ、8つの場面別構成レシピ内蔵。
  4K PNG / PDF / Canva・パワポで文字を直せるPPTX に書き出す。
  「スライド作って」「資料作って」「デッキ作成」「提案資料」「〜のテーマでスライド」
  「Canvaで編集できる形で」「パワポで渡して」「pptxで」で起動。
---

# SlideSmith — 型を選んで言葉を埋めるだけでプロ品質

## 最初に読む：このスキルの決まり（5つ）
1. **HTMLもCSSも書かない。** 書くのは `decks/<案件>/deck.json` だけ。見た目は `engine/` と `core/engine.css` が決める
2. **スクリプトが止めたら、表示された「直し方」どおりに deck.json を直す。** 全部通るまで先へ進まない
3. **台本に無い数字・金額・お客様の声・肩書き・画像を作らない。** 無ければその枠を空けるか、その枠が要らない型にする
4. **文字数が上限を超えたら削る。** 小さくして詰め込む手段は無い。削れないなら2枚に分ける
5. **画像を全枚 Read で開いて見るまで「できました」と言わない。**

## 使うファイル
| ファイル | いつ読む |
|---|---|
| `reference/scenes.md` | STEP 1・2 で必ず読む（営業提案・講座・会社説明/採用・社内報告・ピッチ・告知・研修・事例の構成レシピ） |
| `reference/layouts.md` | STEP 2・3 で必ず読む（型の決定表・枠の文字数上限・アイコン名） |
| `reference/examples/*/deck.json` | STEP 3 の前に1つ読む（書き方の見本。形式を真似る） |
| `reference/reviewer.md` | STEP 5 でレビュー役に渡す |
| `reference/rubric.md` | 採点表。レビュー役が使う（作り手は読まなくてよい） |

---

## STEP 0. 準備（初回だけ）
```bash
cd <このスキルのフォルダ>
npm install          # playwright-core・pptxgenjs が入る。Google Chrome が必要
```

## STEP 1. 聞くこと（台本から分かることは聞かない）
1. **台本**（貼り付け・ファイル・メモ何でもよい）
2. **場面**（`reference/scenes.md` の A〜H から選ぶ。台本と依頼の様子から分かれば聞かない）
3. **テーマ**（下表。場面のレシピに推奨テーマがある。指定が無ければ内容から1つ選び、選んだ理由を1行添えて進める）
4. **ブランド表記**（左下の名前。例: `サービス名 ｜ 会社名`）
5. **書き出し形式**（PNG / PDF / PPTX。指定が無ければ PNG＋PDF）
6. **使ってよい画像**（画面スクショ・写真。無ければ画像を使う型を選ばない）
7. **動画にするか**（ナレーション動画ツール Slide2Video に入れるなら `"video": true`。依頼に「動画」「ナレーション」「テロップ」が出たら聞かずに付ける）

| テーマ | 雰囲気 | 向いている用途 |
|---|---|---|
| fresh | 白×スカイブルー・透明感 | SaaS・サービス説明・採用 |
| corporate | ネイビー×ティール・誠実 | 提案書・会社説明・セミナー |
| duotone | 白×電気的ブルー・幾何学 | スタートアップ・クリエイティブ |
| dynamic | 白黒×マゼンタ・大胆 | ビジョン・スポーツ・行動指針 |
| pop | クリーム×コーラル・丸ゴ | SNS講座・イベント・キャンペーン |
| warm | 生成り・低彩度 | カフェ・食・ワークショップ |
| feminine | くすみピンク×金 | 美容・サロン・女性向け講座 |
| tech | ダーク×シアン | AI・エンジニア向けピッチ |
| minimal | 白黒・削ぎ落とし | デザイン・建築 |
| retro | 臙脂×芥子・昭和 | 商店街・個人店 |
| elegant | 明朝×金・和モダン | 旅館・高級店（**明朝が合わない依頼では使わない**） |
| medical | 白×ミント・清潔 | 治療院・クリニック・健康講座 |
| estate | ベージュ×チャコール×木 | 不動産・物件紹介・工務店 |
| finance | アイボリー×深緑×真鍮・影なし | 税理士・保険・金融・コンサル |
| kids | 原色×丸ゴ×太枠 | 習い事・塾・子育て講座 |
| public | 白×青緑・UD書体 | 自治体・学校・住民説明会 |
| industrial | 白×安全イエロー×黒 | 建設・製造・物流・安全講習 |
| resort | 砂色×ターコイズ | 観光・ホテル・地域PR |
| fitness | 白×オレンジ×墨・斜め | ジム・運動指導・スポーツ教室 |
| gradient | 紫→ピンクのグラデ | AIサービス・新商品発表・SNS発信 |
| luxury | 黒×金・ゴシック | 高額講座・会員制・ハイエンド |
| neon | 黒紫×ピンク＋シアン発光 | ゲーム・eスポーツ・夜イベント |
| nordic | グレージュ×くすみブルー・余白 | インテリア・雑貨・ライフスタイル |

## STEP 2. 構成を決める（1枚＝言いたいこと1つ）
台本を読み、下の表を**頭の中ではなく実際に書き出す**。10枚を超えるならこの表をユーザーに見せて承認を取る。

| # | このスライドで言いたい一文（述語まで） | 中身の形 | 型 |
|---|---|---|---|
| 1 | 〇〇で△△が□□になる | デッキの1枚目 | cover |
| 2 | … | 章が4つ | agenda |

- **出発点は場面のレシピの型の並び**（`reference/scenes.md`）。台本に無い要素の枚は飛ばす
- **1枚ずつの型は `reference/layouts.md` の決定表で選ぶ**（中身の形 → 型。上から最初に当てはまったもの）
- 構成の決まり（違反すると STEP 4 で止まる）:
  - 1枚目 `cover`・最後 `closing`・6枚以上なら2枚目 `agenda`
  - 同じ型は2枚連続まで
  - 本文（cover / agenda / section / message / photo / question / quote / closing 以外）の **60%以上を図解の型**（big-number / stats / bars / compare / table / flow / timeline / funnel / pyramid / matrix / screenshot）
  - **割合を満たすために中身の無い図を作らない**（値が全部同じグラフ・台本に無い数字は止まる／不合格）。図解が足りないときは、文章の枚を減らす・まとめる
  - `features` は本文の25%まで
  - 8枚以上なら暗い面（section / message / photo / question / quote / closing）を2枚以上。章の区切りに `section`、山場に `message`
  - `section` の title は `agenda` の項目と同じ言葉。`section` の直後には必ず中身の枚を置く（台本に中身が無い章は章ごと消す）
  - 目次・章扉の見出しの言葉は台本の流れを要約して作ってよい。中身の事実は作らない

## STEP 3. deck.json を書く
置き場所: `decks/<案件名>/deck.json`（画像は `decks/<案件名>/assets/`）

```json
{
  "theme": "fresh",
  "brand": "サービス名 ｜ 会社名",
  "video": false,
  "slides": [
    { "layout": "cover", "title": "…", "subtitle": "…", "icon": "robot" },
    { "layout": "agenda", "title": "…", "items": [ { "title": "…", "desc": "…" } ] }
  ]
}
```

書き方の決まり:
- [ ] 枠の名前と文字数は `reference/layouts.md` の型の欄をそのまま使う（無い枠を作らない）
- [ ] 文字数は **全角=1・半角=0.5**。上限の8割くらいで書くと通りやすい
- [ ] 見出し（title / message）は**言い切る**。「〜を原本と」「〜の」「〜が正」で終わらせない。体言止めにするなら「。」を付ける
- [ ] 強調は `*語*` で1枚1〜2か所。強調するのは数字か、いちばん言いたい語
- [ ] 改行したい所は `\n`（見出しの意味の切れ目で）
- [ ] `highlight` で主役を1つ決める（problem / flow / bars / features / table / funnel / pyramid / matrix）。全部同じ重さにしない
- [ ] 数字は `"1,000枚"` のように単位ごと書く（数字と単位は自動で大小がつく）
- [ ] `notes` にスピーカーノートを書ける（PPTX のノート欄に入る）
- [ ] **動画用（`"video": true`）**: 下20%（864px より下）は動画のテロップが重なるので中身を置かない。ブランド名・ページ番号は出ない。本文の高さが約1割狭くなるので**文字数は上限の7割**で書く。帯に入ると `VIDEO_BAND`、入り切らないと `ZONE_OVERFLOW` で止まる → 項目を減らす・2枚に分ける

## STEP 4. 描画と機械検品
```bash
node scripts/build.mjs decks/<案件名> --check     # ① 事前チェックだけ（速い）
node scripts/build.mjs decks/<案件名>             # ② 描画＋機械検品（PNG）
```
- `✖ 事前チェックで止まりました` → 表示された **どのスライドの・どの枠を・どう直すか** のとおり deck.json を直して①から
- `✖ 必須ゲート違反` → 同じく直して②から。**出た件数が0になるまで繰り返す**
- `△ 減点` → 直せるものは直す（特に `TITLE_SOFT_END`・`HOLLOW_BOXES`・`BOTTOM_SPARSE`・`LAYOUT_*`）。直さないものは報告に書く
- 同じエラーが3回続いたら、文言をいじり続けずに**型を変える**（決定表の「単調になったら」列）

よく出るエラーと直し方:
| コード | 意味 | 直し方 |
|---|---|---|
| 文字数（上限N字） | 枠に入らない | 削る。削れないなら項目を減らすか2枚に分ける |
| 文の途中で終わっている | 見出しが助詞で終わる | 述語まで書く／言い切りなら「。」 |
| 同じ型が3枚連続 | 単調 | 真ん中を別の型に |
| 図解の型が本文の◯% | 文章の枚が多い | 数字→big-number/bars、違い→compare、順番→flow |
| `ZONE_OVERFLOW` / `BOX_OVERLAP` | はみ出し・重なり | 本文を削る／任意の枠（lead・note・goal）を外す／項目を減らす |
| `BOTTOM_EMPTY` / `HOLLOW_BOXES` | 下や枠の中が空く | 台本に数字があれば各項目に `stat` を足す／中身の多い型へ変える／項目を減らす |
| 棒の値が全部同じ | 数字の無いグラフ | bars をやめ、checklist / problem / flow にする |
| 章扉の後に中身が無い | 空の章 | 中身を入れるか、章扉と目次の項目を消す |
| 画像ファイルが無い | 画像の枠に実物が無い | assets/ に実物を置く。無ければ画像を使わない型へ |

## STEP 5. 目で見る → 別エージェントの採点（最大2ラウンド）
1. `decks/<案件名>/out/sheet.png` と **全スライドの PNG を Read で開く**。次の4点で明らかにおかしい所があれば STEP 3 へ戻る
   - 文字が途中で切れている・重なっている・枠からはみ出している
   - 主役（いちばん大きい要素）が言いたいことと合っていない
   - 3枚以上同じ見た目が続いて見える
   - 台本と違う事実が書かれている
2. `cp decks/<案件名>/deck.json decks/<案件名>/deck.r0.json`
3. **レビュー役を別エージェントで起動**（model: sonnet 以上）。渡す文面:
   ```
   reference/reviewer.md の指示に従い、decks/<案件名>/ のスライドを採点して JSON だけ返してください。
   台本: <台本のパス or 本文>
   ```
4. 返ってきた `changes` を deck.json に反映 → STEP 4 を必須ゲート全合格まで → `deck.r1.json` に保存 → もう一度レビュー
5. **2ラウンドで打ち切る。** 点が下がったら一番高かった版の deck.json に戻す
6. 合格ライン: 必須ゲート全合格 ＋ 総合80点以上 ＋ 60%未満のスライド無し

## STEP 6. 書き出して報告する
```bash
node scripts/build.mjs decks/<案件名> --pdf              # PNG＋PDF
node scripts/build.mjs decks/<案件名> --pptx             # ＋編集できるPPTX
node scripts/pptx-preview.mjs decks/<案件名>/out/<案件名>.pptx   # PPTXを実際にKeynote/PowerPointで描画して確認
```
- PPTX は「背景＝画像、文字＝テキストボックス」。パワポ/Keynote/Canva で文字を直せる。**図形の位置や色は直せない**（直したいときは deck.json を直して書き出し直す）
- PPTX の文字は `Noto Sans JP`。入っていないPCでは近い書体に置き換わる（※などの記号が□になることがある）。Canva には入っている
- `out/preview/` の PNG を開き、`out/NN_*.png` と見比べて文字のずれ・欠けが無いか確かめる
- 報告に書くこと: 枚数・テーマ・出力ファイルのパス・レビューの総合点・直さなかった減点・台本から削った内容

---

## 画像を使うとき
- **画面スクショ**（screenshot 型）: 実物の画面を `assets/` に置く。横1600px以上が目安（小さいと `IMG_LOWRES`）
- **写真**（photo 型・cover の image・profile / testimonial の image）: 実物の写真か、生成した写真を `assets/` に置く
- 写真の生成（人物の顔アップは崩れやすい。風景・物・後ろ姿・手元が安定）:
  ```bash
  node scripts/genimg.mjs "<日本語で場面を描写>" decks/<案件>/assets/xxx.jpg --style <テーマ名> --ar 16:9 --free     # 無料
  node scripts/genimg.mjs "<場面>" decks/<案件>/assets/xxx.png --style <テーマ名> --ar 16:9 --openai                  # gpt-image-2（要設定・約6〜10円/枚）
  ```
  - キーの設定は `node scripts/setup-openai.mjs` / `node scripts/setup-gemini.mjs` を**ユーザー自身のターミナル**で実行してもらう（キーをチャットに貼らせない）
  - 文字を画像に入れさせない（スライドの文字は必ず deck.json に書く）
- お客様の写真・声・ロゴは**使ってよいと確認が取れたものだけ**

## してはいけないこと
- `engine/` `core/` `themes/` `scripts/` を案件のために書き換える（全デッキに影響する）
- 生成された `html/` を手で直す（次のビルドで消える。deck.json を直す）
- 必須ゲート違反が残ったまま書き出す・報告する
- 台本に無い数字・金額・実績・お客様の声・肩書きを書く
- 画像が無いのに画像を使う型を選ぶ（空の枠は不合格の典型）
- 画像を開かずに「完成しました」と報告する

## 旧ルート（過去のデッキを直すときだけ）
2026年9月より前に作ったデッキは、HTML手書き（`core/layouts.md` ＋ `scripts/render.mjs`）か、8型の PPTX（`scripts/pptx.mjs`・`examples/pptx-sample/`）で作られている。
その修正はそれぞれの旧ルートで行う。**新しいデッキは必ずこのファイルの手順で作る。**
