# 型の決定表と枠の一覧

> このファイルは `node scripts/layouts-doc.mjs` で自動生成（上半分の決定表だけは scripts/layouts-doc.mjs 内で編集）。

## 1. 決定表 — 中身の形から型を選ぶ

上から順に当てはまるか見て、最初に当てはまった型を使う。

| 中身の形（台本のどこを見るか） | 使う型 | 単調になったら（直前と同じ型のとき） |
|---|---|---|
| デッキの1枚目 | cover | — |
| 6枚以上のデッキの2枚目。章が3〜6個 | agenda | — |
| 章の切り替わり | section | — |
| 一番伝えたい数字が1つある（実績・量・削減） | big-number | message（evidence に数字） |
| 前と後・AとB・役割分担など「2つを並べて違いを見せる」 | compare（mode を選ぶ） | problem |
| 主役の数字が2〜4個並ぶ（会社の規模・KPI・実績） | stats | big-number（1つに絞る） |
| 数量が2〜6個あり、大小・推移を比べたい | bars（項目の比較=bar / 時期の推移=column） | big-number（1つに絞る） |
| 「項目：内容」の一覧（会社概要・開催概要・募集要項・症状→原因のような2列の対応） | spec | table（3列以上なら） |
| 2〜4つを複数の観点で見比べる（方式・プランの機能比較）。columns=比べる対象、rows.label=観点、cells=各対象の値 | table | compare（2つに絞る） |
| 段階ごとに数が減っていく（認知→問い合わせ→成約） | funnel | flow |
| 土台の上に積み上がる・階層・優先順位 | pyramid | features |
| 2つの軸で4つに分ける（位置づけ・どこから始めるか） | matrix | compare（mode=split） |
| 順番がある（手順・処理の流れ・判断の順） | flow | timeline（時期があるなら） |
| 時期・日付・フェーズがある（予定・ロードマップ） | timeline | flow |
| 困りごと・課題・なぜ難しいか（2〜4個） | problem | features |
| 実際の画面を見せたい、**かつ画像ファイルがある** | screenshot | 画像が無ければ compare / flow（空の枠は禁止） |
| いちばん言いたい一文・方針・まとめの一言 | message | photo（実物の写真があるとき） |
| 実物の写真で現場・空気を伝える、**かつ写真ファイルがある** | photo | message |
| 料金・プランを提示する（金額が決まっている） | pricing | table |
| 実在の利用者の声・導入事例（本人の言葉がある） | testimonial | big-number（成果の数字だけ） |
| 話し手・担当者を**1人**紹介 | profile | — |
| メンバーを**2〜4人**紹介 | team | — |
| 聞き手に考えてもらう問い・手を動かすワーク・クイズ | question | message |
| 実在の人の言葉を大きく見せる（出典がある引用） | quote | testimonial |
| 明日からやること・持ち物・作業前の確認 | checklist | summary |
| 導入前の不安・よくある質問（2〜4問） | faq | problem |
| 締めの直前の振り返り（要点3〜4個） | summary | message |
| 順序のない要点3〜4個（強み・ルール・安全策） | features（本文の25%まで） | problem（icon を付ける） |
| 最後の1枚。次にしてほしい行動 | closing | — |

### デッキ全体の決まり（事前チェックと機械検品が止める）
- 1枚目 cover、最後 closing。6枚以上なら2枚目 agenda
- **同じ型は2枚連続まで**（3枚目は「単調になったら」列の型へ）
- 本文スライド（cover / agenda / section / message / photo / question / quote / closing 以外）のうち **図解の型（big-number / stats / compare / flow / timeline / screenshot / bars / table / funnel / pyramid / matrix）を60%以上**。40%未満は止まる
- features（カード並べ）は本文の25%まで
- 8枚以上のデッキは暗い面（section / message / closing）を2枚以上。章の区切りに section、山場に message
- section の title は agenda の項目と同じ言葉にする。**section の直後は必ず中身のスライド**（章扉が続く・章扉のすぐ後が締めは止まる）
- **図解の割合を満たすために中身の無い図を作らない**（値が全部同じグラフは止まる。台本に数字が無ければ数字の型を選ばない）

### 言葉の決まり
- 見出し（title / message）は**述語まで書き切る**。「〜を原本と」「〜が正」のように途中で終わると止まる。言い切りの体言止めや「〜に。」は「。」を付ける
- 強調は `*語*` で1枚1〜2か所。改行したい位置は `\n`
- 文字数が上限を超えたら**削る**。削れないなら項目を減らすか、2枚に分ける（小さくして詰め込む手段は無い）
- 目次・章扉の**見出しの言葉**は台本の流れを要約して作ってよい（構造の言葉）。ただし中身の事実は作らない
- 数字・金額・お客様の声・肩書きは台本にあるものだけ使う。台本に無いものを作らない（bars / funnel / pricing / testimonial / profile は特に注意）
- 画像を使う型（screenshot / photo / 写真つき profile・testimonial）は assets/ に実物があるときだけ

## 2. 型ごとの枠

### cover — 表紙（明るい面）
デッキの1枚目。何の資料で、相手に何の得があるか

- `kicker`（任意）24字
- `title`（必須）6〜30字 — 相手の得が読めるタイトル。2行以内
- `subtitle`（任意）70字
- `meta`（任意）30字 — 例: 2026.09 ご説明資料
- `image`（任意）assets/ の画像パス。実物が無いと事前チェックで止まる
- `mark`（任意）4字 — 画像が無いときの透かし文字（例: AI）
- `icon`（任意）アイコン名（末尾の一覧から）

### agenda — 目次（明るい面）
5枚を超えるデッキの2枚目。章は3〜6個

- `kicker`（任意）20字
- `title`（必須）24字
- `items`（必須）3〜6個
  - `title`（必須）16字
  - `desc`（任意）30字

### section — 章扉（暗い面）
章の切り替わり。目次の章名と同じ言葉にする

- `title`（必須）20字
- `lead`（任意）50字

### problem — 課題提起（明るい面）
「困っていること」「なぜ難しいか」を2〜4個。数字か具体場面を入れる

- `kicker`（任意）24字
- `title`（必須）28字
- `lead`（任意）56字
- `items`（必須）2〜4個
  - `head`（必須）14字
  - `body`（必須）46字
  - `stat`（任意）8字 — 例: 1,000枚
  - `statLabel`（任意）12字
  - `icon`（任意）アイコン名（末尾の一覧から）
- `highlight`（任意）数字 1〜4

### message — キーメッセージ（暗い面）
いちばん伝えたい一文。根拠の数字を1〜3個添えられる。デッキの山場に

- `kicker`（任意）24字
- `message`（必須）6〜40字
- `sub`（任意）40字
- `evidence`（任意）0〜3個
  - `value`（必須）8字
  - `label`（必須）22字

### big-number — 巨大数字（明るい面）
一番伝えたい数字が1つあるとき（実績・削減量・規模）。比較（前→後）を添えると強い

- `kicker`（任意）24字
- `title`（必須）28字
- `body`（必須）20〜80字
- `note`（任意）40字 — 出典・期間
- `value`（必須）7字 — 例: 1,000 / 38 / 3
- `unit`（任意）4字 — 例: 枚 / % / 倍
- `label`（必須）18字
- `from`（任意）10字
- `to`（任意）10字
- `ring`（任意）数字 0〜100
- `chips`（任意）0〜2個
  - `value`（必須）7字
  - `label`（必須）14字
  - `icon`（任意）アイコン名（末尾の一覧から）

### compare — 比較（明るい面）
Before→After（mode=before-after）／A vs B（versus）／役割分担など対等な2つ（split）

- `kicker`（任意）24字
- `title`（必須）30字
- `lead`（任意）56字
- `mode`（任意）次から1つ: before-after / versus / split
- `left`（必須）
  - `label`（必須）16字
  - `value`（任意）8字
  - `items`（必須）2〜5個 ／1個26字まで
- `right`（必須）
  - `label`（必須）16字
  - `value`（任意）8字
  - `items`（必須）2〜5個 ／1個26字まで
- `verdict`（任意）36字

### flow — 手順フロー（明るい面）
順番のある手順・処理の流れ（3〜5段）。最後にゴールを置ける

- `kicker`（任意）24字
- `title`（必須）28字
- `lead`（任意）56字
- `steps`（必須）3〜5個
  - `head`（必須）12字
  - `body`（必須）40字
  - `tag`（任意）12字
  - `icon`（任意）アイコン名（末尾の一覧から）
- `highlight`（任意）数字 1〜5
- `goal`（任意）28字

### timeline — ロードマップ（明るい面）
時期のある予定・フェーズ（3〜5個）。現在地を now で示す

- `kicker`（任意）24字
- `title`（必須）28字
- `lead`（任意）56字
- `items`（必須）3〜5個
  - `when`（必須）10字
  - `head`（必須）14字
  - `body`（必須）36字
- `now`（任意）数字 1〜5

### features — 特徴・決めごと（明るい面）
順序のない要点3〜4個（強み・ルール・安全策）。各項目にアイコン必須。多用禁止（本文の25%まで）

- `kicker`（任意）24字
- `title`（必須）28字
- `lead`（任意）56字
- `items`（必須）3〜4個
  - `icon`（必須）アイコン名（末尾の一覧から）
  - `head`（必須）14字
  - `body`（必須）44字
  - `stat`（任意）8字
- `highlight`（任意）数字 1〜4

### screenshot — 画面紹介（明るい面）
実際の画面を見せる。画像ファイルが無いときは使えない（別の型を選ぶ）

- `kicker`（任意）24字
- `title`（必須）28字
- `lead`（任意）56字
- `image`（必須）assets/ の画像パス。実物が無いと事前チェックで止まる
- `url`（任意）40字
- `points`（必須）2〜3個
  - `head`（必須）14字
  - `body`（必須）40字

### bars — グラフ（棒）（明るい面）
数量を比べる・推移を見せる（2〜6本）。言いたい棒を highlight、結論を takeaway に。数字は実数だけ

- `kicker`（任意）24字
- `title`（必須）28字
- `lead`（任意）56字
- `mode`（任意）次から1つ: bar / column
- `unit`（任意）4字 — 例: 時間 / 件 / %
- `items`（必須）2〜6個
  - `label`（必須）12字
  - `value`（必須）数字 0〜1000000000000
  - `display`（任意）10字 — 棒に出す文字。省略時は value＋unit
- `highlight`（任意）数字 1〜6
- `takeaway`（任意）40字 — グラフから言えること。例: 月40時間が8時間に
- `note`（任意）40字 — 出典・期間

### table — 表（明るい面）
2〜4つを3つ以上の観点で見比べる（プラン・方式・条件の一覧）。セルは短い言葉か ○ △ ×。highlight で推す列

- `kicker`（任意）24字
- `title`（必須）28字
- `lead`（任意）56字
- `columns`（必須）2〜4個 ／1個12字まで
- `rows`（必須）2〜6個
  - `label`（必須）12字
  - `cells`（必須）2〜4個 ／1個18字まで
- `highlight`（任意）数字 1〜4
- `note`（任意）40字

### pricing — 料金プラン（明るい面）
料金・プランを2〜3個。おすすめを recommend で1つ強調。金額は決まっている実際の数字だけ

- `kicker`（任意）24字
- `title`（必須）28字
- `lead`（任意）56字
- `plans`（必須）2〜3個
  - `name`（必須）12字
  - `price`（必須）10字 — 例: 220,000
  - `unit`（任意）10字 — 例: 円（税込）
  - `desc`（任意）30字
  - `items`（必須）2〜5個 ／1個22字まで
- `recommend`（任意）数字 1〜3
- `note`（任意）50字

### pyramid — 構造図（ピラミッド）（明るい面）
土台の上に積み上がる関係・階層（3〜5段）。上から順に書く（上ほど目的・少数、下ほど土台）

- `kicker`（任意）24字
- `title`（必須）28字
- `lead`（任意）56字
- `layers`（必須）3〜5個
  - `head`（必須）12字
  - `body`（必須）40字
- `highlight`（任意）数字 1〜5

### matrix — 2×2マトリクス（明るい面）
2つの軸で4つに分ける（位置づけ・優先順位・狙う所）。cells は 左上→右上→左下→右下 の順

- `kicker`（任意）24字
- `title`（必須）28字
- `lead`（任意）56字
- `x`（必須）
  - `label`（必須）10字
  - `low`（必須）8字
  - `high`（必須）8字
- `y`（必須）
  - `label`（必須）10字
  - `low`（必須）8字
  - `high`（必須）8字
- `cells`（必須）4〜4個
  - `head`（必須）12字
  - `body`（必須）30字
- `highlight`（任意）数字 1〜4

### funnel — ファネル（明るい面）
段階ごとに数が絞られていく流れ（認知→問い合わせ→成約など3〜5段）。数字は実数だけ

- `kicker`（任意）24字
- `title`（必須）28字
- `lead`（任意）56字
- `stages`（必須）3〜5個
  - `label`（必須）10字
  - `value`（任意）10字 — 例: 1,200人 / 38%
  - `body`（必須）36字
- `highlight`（任意）数字 1〜5

### testimonial — お客様の声・事例（明るい面）
実在の利用者の声・導入事例。発言は本人の言葉のまま（作らない）。成果の数字があれば results に

- `kicker`（任意）24字
- `title`（必須）28字
- `quote`（必須）10〜90字
- `name`（必須）20字 — 許可を取った表記。例: A治療院 院長
- `role`（任意）30字
- `image`（任意）assets/ の画像パス。実物が無いと事前チェックで止まる
- `results`（任意）0〜3個
  - `value`（必須）8字
  - `label`（必須）16字

### profile — プロフィール（明るい面）
話し手・会社・担当者の紹介。実績の数字と肩書きは事実だけ

- `kicker`（任意）24字
- `title`（必須）20字 — 例: 講師紹介 / 私たちについて
- `name`（必須）16字
- `role`（必須）30字
- `image`（任意）assets/ の画像パス。実物が無いと事前チェックで止まる
- `bio`（必須）20〜100字
- `facts`（任意）0〜3個
  - `value`（必須）8字
  - `label`（必須）16字
- `tags`（任意）0〜4個 ／1個12字まで

### photo — 写真＋メッセージ（暗い面）
実物の写真1枚で空気・現場を伝える山場。写真ファイルが無いときは使えない（message を使う）

- `kicker`（任意）24字
- `message`（必須）6〜26字 — 1行14字×2行まで
- `sub`（任意）50字
- `image`（必須）assets/ の画像パス。実物が無いと事前チェックで止まる
- `credit`（任意）30字

### faq — よくある質問（明るい面）
導入前の不安に先回りして答える（2〜4問）。答えは結論から書く

- `kicker`（任意）24字
- `title`（必須）28字
- `items`（必須）2〜4個
  - `q`（必須）30字
  - `a`（必須）64字

### summary — まとめ（明るい面）
締めの直前に要点を3〜4個で振り返る。各要点は言い切りの一文

- `kicker`（任意）24字
- `title`（必須）24字
- `points`（必須）3〜4個
  - `head`（必須）20字
  - `body`（任意）40字
- `next`（任意）30字 — 次にしてほしいこと

### stats — 数字で見る（明るい面）
主役の数字が2〜4個並ぶとき（会社の規模・KPI・実績）。1つだけなら big-number。数字は実数だけ

- `kicker`（任意）24字
- `title`（必須）28字
- `lead`（任意）56字
- `items`（必須）2〜4個
  - `value`（必須）8字 — 例: 1,200社 / 98%
  - `label`（必須）16字
  - `note`（任意）24字 — 前年比・期間など
  - `icon`（任意）アイコン名（末尾の一覧から）
- `highlight`（任意）数字 1〜4
- `note`（任意）40字 — 出典・時点

### spec — 概要（項目と内容）（明るい面）
会社概要・開催概要・募集要項など「項目：内容」の一覧（3〜7行）。行ごとに短く

- `kicker`（任意）24字
- `title`（必須）24字
- `rows`（必須）3〜7個
  - `key`（必須）8字 — 例: 日時 / 所在地
  - `value`（必須）40字
- `icon`（任意）アイコン名（末尾の一覧から）
- `image`（任意）assets/ の画像パス。実物が無いと事前チェックで止まる
- `cta`（任意）24字 — 申し込み方法など1行

### team — チーム紹介（明るい面）
2〜4人のメンバー紹介。1人だけなら profile。肩書き・経歴は事実だけ。写真が無ければ頭文字の丸になる

- `kicker`（任意）24字
- `title`（必須）24字
- `lead`（任意）56字
- `members`（必須）2〜4個
  - `name`（必須）12字
  - `role`（必須）20字
  - `bio`（必須）44字
  - `image`（任意）assets/ の画像パス。実物が無いと事前チェックで止まる

### question — 問いかけ・ワーク（暗い面）
聞き手に考えてもらう問い・手を動かすワーク。時間とやることを添えられる（講座・研修・クイズ）

- `kicker`（任意）24字 — 例: WORK / QUESTION
- `question`（必須）6〜32字 — 「？」で終わる問い、または「〜を書き出す」。steps を付けるなら24字まで
- `sub`（任意）50字
- `time`（任意）8字 — 例: 3分
- `steps`（任意）0〜3個 ／1個26字まで

### checklist — チェックリスト（明るい面）
明日からやること・持ち物・作業前の確認など、チェックして使う項目（3〜6個）

- `kicker`（任意）24字
- `title`（必須）28字
- `lead`（任意）56字
- `items`（必須）3〜6個
  - `head`（必須）20字
  - `body`（任意）36字
- `note`（任意）40字

### quote — 引用・名言（暗い面）
実在の人の言葉を大きく見せる（講演・インタビュー・事例）。発言者と出典がある言葉だけ。自分たちの主張なら message

- `kicker`（任意）24字
- `quote`（必須）6〜60字
- `name`（必須）20字
- `role`（任意）30字
- `image`（任意）assets/ の画像パス。実物が無いと事前チェックで止まる

### closing — 締め・行動喚起（暗い面）
最後の1枚。次にしてほしい行動を1つだけ

- `kicker`（任意）20字
- `title`（必須）24字
- `subtitle`（任意）70字
- `cta`（必須）26字 — 動詞で終わる行動。例: デモを触ってみる
- `contact`（任意）40字

## 3. アイコン名
`rocket` `chart-up` `bar-chart` `clock` `bulb` `target` `gear` `mail` `smartphone` `laptop` `book` `key` `shield` `heart` `star` `speech` `person` `people` `house` `coffee` `sprout` `music` `camera` `trophy` `document` `check` `lock` `calendar` `flag` `search` `alert` `yen` `calculator` `upload` `robot` `arrow`
